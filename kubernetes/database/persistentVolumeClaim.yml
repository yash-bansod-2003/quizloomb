apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: quizloom-database-pvc
  namespace: quizloom
spec:
  accessModes:
    - ReadWriteOnce
  volumeMode: Filesystem
  resources:
    requests:
      storage: 1Gi
  storageClassName: local-storage
