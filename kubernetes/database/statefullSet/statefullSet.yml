apiVersion: apps/v1
kind: StatefulSet

metadata:
  name: quizloom-database-statefulset
  namespace: quizloom

spec:
  serviceName: quizloom-database-service
  replicas: 2
  selector:
    matchLabels:
      app: quizloom-database
  template:
    metadata:
      labels:
        app: quizloom-database
    spec:
      containers:
        - name: quizloom-database
          image: postgres:17.2
          ports:
            - containerPort: 5432
          env:
            - name: POSTGRES_USER
              valueFrom:
                configMapKeyRef:
                  name: quizloom-database-config-map
                  key: POSTGRES_USER
            - name: POSTGRES_PASSWORD
              valueFrom:
                configMapKeyRef:
                  name: quizloom-database-config-map
                  key: POSTGRES_PASSWORD
            - name: POSTGRES_DB
              valueFrom:
                configMapKeyRef:
                  name: quizloom-database-config-map
                  key: POSTGRES_DB
          volumeMounts:
            - name: quizloom-database-data
              mountPath: /var/lib/postgresql/data
  volumeClaimTemplates:
    - metadata:
        name: quizloom-database-data
      spec:
        accessModes: ["ReadWriteOnce"]
        resources:
          requests:
            storage: 1Gi
